# Runtime budgets per verification profile, in seconds. Enforced by RULE-GATE-001.
#
# A slow gate gets bypassed, which is worse than no gate: it teaches contributors that
# green is optional. Raising a budget means raising it here, saying why in a decision
# record, and doing both in the same commit.

check=180
verify=600
