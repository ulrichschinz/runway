# Declared runtime versions for this repository — the single source of truth.
#
# RULE-TI-002 checks that the Dockerfiles agree with these values. Changing a version
# here means changing the Dockerfile, the rule's fixture, and a decision record in the
# same commit (the meta-rule).

PYTHON_VERSION=3.12
NODE_VERSION=20
