JWT_SECRET=replace-with-a-long-random-string
ALLOW_REGISTRATION=true
PORT=4000

# Webhook endpoint (POST /inbox) — for agents/automations
# Generate a secret: openssl rand -base64 32
WEBHOOK_SECRET=
WEBHOOK_USER=uli
