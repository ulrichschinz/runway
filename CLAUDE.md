# CLAUDE.md

This file carries **no rules of its own**. It exists so that tools looking for a
vendor-specific file find the agent-agnostic one instead.

**Read [`AGENTS.md`](AGENTS.md).** It is the contract: the decision procedure, the
structure rules, the public surfaces, and the meta-rule that binds changes to them.
