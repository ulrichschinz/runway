fastapi==0.115.5
uvicorn[standard]==0.53.0
# 3.3.0 carried the algorithm-confusion and JWT-bomb advisories (finding SEC-7). 3.5.0 was
# verified drop-in against this application's exact usage — HS256 encode/decode, and an
# `alg: none` token still refused — on 2026-08-26.
python-jose[cryptography]==3.5.0

# passlib 1.7.4 is the LATEST release, and it is from 2020-10-08. Unmaintained, not merely
# pinned. See ADR 0021 and RISK-DEP-003.
passlib[bcrypt]==1.7.4

# LOAD-BEARING PIN, and not for the reason it looks like. passlib reads
# bcrypt.__about__.__version__, which bcrypt removed. Tested against bcrypt 5.0.0 on
# 2026-08-26: it does not warn, it FAILS — `ValueError: password cannot be longer than 72
# bytes` on every hash, because bcrypt 5 dropped the silent truncation passlib relies on.
# Bumping this breaks every login. Dependabot will offer 5.0.0; do not take it without
# replacing passlib first.
bcrypt==4.0.1
python-multipart==0.0.12
pydantic-settings==2.6.1
aiosqlite==0.20.0
# 0.3.3 held here until 2026-09-18, and for most of that time the MCP server did not work.
# It declares `mcp>=1.6.0` with no upper bound, and the SDK changed the wire wrapper inside
# that range: 0.3.3 feeds a bare JSONRPCMessage to a receive loop expecting SessionMessage,
# so the session is dropped at `initialize` with `'JSONRPCMessage' object has no attribute
# 'message'`. The application still imports and boots, which is why nothing caught it —
# ADR 0004's pin fixed the boot crash and left the runtime break in place. 0.4.0 requires
# mcp>=1.12.0, speaks the current wrapper, and adds the header allowlist app/main.py needs.
# See ADR 0033, and tests/unit/test_mcp_session.py, which now fails if this regresses.
fastapi-mcp==0.4.0
# The upper bound survives for its original reason: mcp 2.0.0 changed Server.__init__ and
# makes `import app.main` raise TypeError, so a fresh build produces an image whose backend
# cannot start. Pinned until fastapi-mcp supports mcp 2.x. See ADR 0004.
mcp==1.29.0
